.venv
__pycache__
*.pyc
*.pyo
*.pyd
.Python
env
venv
bin
lib
include
.git
.dockerignore
Dockerfile
.env*
.vscode
.idea

# Data & Samples (Sensitive/Heavy)
voice_samples/
custom_voice/
recordings/
wake_up_file/
tests/
scripts/*
!scripts/bootstrap/
scripts/bootstrap/*
!scripts/bootstrap/sovits_bootstrap.sh
# P3-3 (M1-A4): main.py imports this at module level (Provisioning Guard),
# so excluding it made the signaling server's image unable to even start --
# confirmed live: ModuleNotFoundError on every container start, the first
# time this service was ever actually deployed via compose.
!scripts/bootstrap/provision_models.py
# The HTTP routers also import these lightweight runtime helpers. Keep the
# rest of the interactive/diagnostic scripts out of the slim image.
!scripts/audio/
scripts/audio/*
!scripts/audio/record_voice.py
!scripts/export_friend.py
!scripts/import_friend.py
!scripts/validate_persona_file.py
models/
checkpoint*/
*.wav
*.mp3
*.mp4
*.m4a
*.ppn
*.json
!package.json
!tsconfig.json
# re-include specific config jsons if needed, but exclude personal ones
!app/agents/personality.json
# history.json is personal, keep ignored.

history.json
personality.json
backups/
_archive/

# Local persistence & Caches
database/
chroma_db/
.cache/
.huggingface/
.pytest_cache/
