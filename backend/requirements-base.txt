python-dotenv>=1.2.2,<2.0.0
numpy>=2.2.6,<3.0.0
# scripts/audio/record_voice.py (Phase 2.3 voice enrollment) -- a real runtime
# dependency of that entry point, never previously declared anywhere. It ran
# only by accident, if something else happened to pull these in transitively.
sounddevice>=0.5.0,<1.0.0
soundfile>=0.13.0,<1.0.0
asyncpg>=0.31.0,<1.0.0
pydantic>=2.13.4,<3.0.0
pydantic-settings>=2.14.1,<3.0.0
fastapi>=0.136.1,<1.0.0
uvicorn>=0.47.0,<1.0.0
websockets>=16.0,<17.0
jaraco.context>=6.1.2,<7.0.0
wheel>=0.47.0,<1.0.0
nats-py>=2.14.0,<3.0.0
neo4j>=6.2.0,<7.0.0
# P3-3 (M1-A4): scripts/bootstrap/provision_models.py (imported by main.py's
# Provisioning Guard, so this is a real runtime dependency of the signaling
# service, not just a dev script) uses `requests` for its streamed tar.bz2
# download -- it was never declared here, only ever working by riding along
# as some other package's transitive dependency. Confirmed missing live:
# ModuleNotFoundError the first time main.py actually ran under the `slim`
# Docker target, which has no such transitive pull.
httpx>=0.28.1,<1.0.0
requests>=2.34.2,<3.0.0
livekit-api>=1.1.0,<2.0.0
livekit>=1.1.8,<2.0.0
python-json-logger>=4.1.0,<5.0.0
orjson>=3.10.0,<4.0.0
qdrant-client==1.9.0
redis>=5.0.0,<6.0.0
